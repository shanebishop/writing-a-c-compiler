[workspace]
resolver = "3"
members = ["driver", "errors", "lexer", "parser", "fuzz", "common"]

[profile.dev]
debug = "line-tables-only" # For speeding up local builds; comment out for serious debugging
