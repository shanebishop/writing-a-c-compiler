[package]
name = "parser"
version = "0.1.0"
edition = "2024"

[dependencies]
common = { path = "../common" }
errors = { path = "../errors" }

[dev-dependencies]
lexer = { path = "../lexer" }

[lints]
workspace = true
